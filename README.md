# botsecurity — Telegram-бот реестра образов ИБ

Публичный Telegram-бот для мониторинга [Google-таблицы](https://docs.google.com/spreadsheets/d/1l-FSeC1mfIXqX-bvoKdfRV5txF0TDQ5aD-8GD8Ey-sw/edit) с образами для проверки ИБ.

## Возможности

- Опрос таблицы каждый час (настраивается)
- Уведомления подписчикам о новых образах и изменениях
- Напоминания в рабочие дни (10:00, 13:00, 16:00, 18:00 МСК) о непереданных образах
- Публичные команды и **кнопки** (нижнее меню + inline)
- Выборка по датам: прошли / не прошли проверку
- **Отчёты ИБ**: пришлите архив сканирования (.7z/.zip) — бот определит вердикты и проставит статусы в таблице
- Зеркалирование старого xls-реестра: `/add` и статусы пишутся в обе таблицы; `/sync` и hourly poll дописывают недостающие теги
- Админ-команды для управления
- Роли ИБ, история строк, CSV-экспорт, SLA-напоминания и настройки уведомлений

## Поиск

Просто напишите текст в чат — бот ищет по тегу, релизу, разработчику и статусу.
Поддерживаются несколько слов: `Зуев api`, `leadgen frontend`.
Если написать дату (`15.06.2026` или `01.06.2026-15.06.2026`) — покажет образы за период.

## Команды

| Команда | Описание |
|---------|----------|
| `/start` | Подписка на уведомления |
| `/help` | Справка |
| `/add` | Добавить тег в реестр (для разработчиков) |
| `/fix` | Исправленный тег: выбрать непрошедший образ |
| `/my` | Мои образы и статусы |
| `/pending` | Образы без статуса / не переданы |
| `/on_review` | Образы на проверке у ИБ |
| `/passed` | Прошли проверку ИБ |
| `/failed` | Не прошли проверку ИБ |
| `/devs` | Разработчики (кнопки → их образы) |
| `/releases` | Релизы (кнопки → образы релиза) |
| `/dates` | Выборка по датам (кнопки) |
| `/find текст` | Поиск по всем полям |
| `/status` | Сводка по статусам |
| `/today` | Добавленные сегодня |
| `/last` | Последние добавленные |
| `/by_dev Зуев` | Образы разработчика |
| `/stale 3` | Висят без статуса ≥ N дней |
| `/subscribe` / `/unsubscribe` | Подписка / отписка |
| `/sync` | *(админ)* Принудительная синхронизация |
| `/stats` | *(админ)* Статистика бота |
| `/users [дней]` | *(админ)* Активность пользователей: кто что запрашивал |
| `/user id` | *(админ)* История действий пользователя |
| `/broadcast текст` | *(админ)* Рассылка всем подписчикам |
| `/history номер_строки` | История изменений строки |
| `/notify all\|mine\|fail\|digest\|off` | Настроить уведомления |
| `/export all\|pending\|failed\|passed` | *(админ)* Выгрузить CSV |
| `/metrics` | *(админ)* Метрики качества и времени проверки |
| `/role id ib_operator` | *(админ)* Выдать роль оператора ИБ |

В каждом списке: нумерованные кнопки открывают карточку образа со всеми полями,
◀️ ▶️ листают страницы. Еженедельный дайджест — по понедельникам в 9:30 МСК.
Роли: `developer` (по умолчанию, может добавлять теги), `ib_operator` (может
обрабатывать отчёты ИБ), `viewer` (только просмотр).

## Отчёты сканирования ИБ (автопроставление статусов)

Администратор присылает боту архив отчёта (.7z или .zip, до 20 МБ) — тот самый,
который отдаёт ИБ (внутри `result/*.json` со сканами Aqua). Бот:

1. Разбирает архив и читает `vulnerability_summary` каждого образа.
2. Вердикт: есть хотя бы 1 **critical** или **high** → «Не прошло проверку», иначе «Прошло проверку».
3. Сопоставляет образы из отчёта со строками таблицы (по имени и версии,
   в приоритете строки без статуса).
4. Показывает превью с вердиктами и кнопкой подтверждения.
5. После подтверждения проставляет статусы и дату проверки в таблице
   и рассылает уведомления подписчикам.

⚠️ Для записи в таблицу нужен `credentials.json` сервисного аккаунта
с правами **редактора** на таблицу. Без него бот покажет вердикты,
но статусы придётся проставить вручную.

Превью отчёта привязано к загрузившему его оператору и действует 15 минут.
Если строка была изменена после предпросмотра, бот отменит применение и
попросит загрузить отчёт заново. Для архивов действуют лимиты: 500 файлов,
100 МБ распакованных данных и 2 МБ на JSON.

## Разработчики: добавление тегов и исправления

Любой пользователь бота может добавить образ в реестр через `/add` или кнопку
«➕ Добавить тег»: тег(и), релиз, фамилия, дата передачи → превью → запись в таблицу.

- `/my` — список ваших образов по сохранённой фамилии
- При смене статуса вашего образа бот присылает **личное** уведомление
- Если проверка **не прошла** — пришлите пересобранный тег (или нажмите кнопку
  в уведомлении). Бот запишет его в столбец «Исправленный тег» и сбросит
  статус на **«не передано»** (дата проверки очищается — образ снова ждёт передачи в ИБ)

## Быстрый старт (Docker на VDS)

Образ собирается в GitHub Actions и лежит в GHCR: `ghcr.io/rsnest/botsecurity:latest`.
На слабой VDS **не билдим** — только pull.

```bash
git clone https://github.com/RsNest/botsecurity.git
cd botsecurity

cp .env.example .env
nano .env

cp credentials.json.example credentials.json
# расшарить таблицы на client_email сервисного аккаунта

# один раз: логин в GHCR (PAT или gh auth token с read:packages)
echo YOUR_GITHUB_TOKEN | sudo docker login ghcr.io -u RsNest --password-stdin

sudo docker compose pull
sudo docker compose up -d
sudo docker compose logs -f
```

### Обновление бота после правок в репо

1. Пушишь в `main` → Actions сам собирает и пушит образ (~1–3 мин).
2. На VDS:

```bash
cd /home/rudolf710/botsecurity
git pull   # подтянуть compose/.env.example при необходимости
sudo docker compose pull
sudo docker compose up -d
```

Локальная сборка на VDS больше не нужна (`build:` закомментирован в compose).

## Переменные окружения (.env)

```env
TELEGRAM_TOKEN=...           # от @BotFather
ADMIN_IDS=145212489          # ваш Telegram user id
BOT_UID=1000                 # Linux Docker-хост: вывод команды id -u
BOT_GID=1000                 # Linux Docker-хост: вывод команды id -g
SPREADSHEET_ID=1l-FSeC1mfIXqX-bvoKdfRV5txF0TDQ5aD-8GD8Ey-sw
SHEET_GID=684739217
SPREADSHEET_MIRROR_ID=1nRDstaHXnZ2Jf9IvgsAAvM8anro9JpjB   # старый xls; пусто = выкл
SHEET_MIRROR_GID=684739217
GOOGLE_CREDENTIALS_PATH=credentials.json
POLL_INTERVAL_MINUTES=60
REMINDER_HOURS=10,13,16,18
TIMEZONE=Europe/Moscow
SLA_PENDING_DAYS=3
```

## Доступ к Google Sheets

**Вариант 1 (рекомендуется):** Service Account

1. [Google Cloud Console](https://console.cloud.google.com/) → новый проект
2. APIs & Services → Enable **Google Sheets API**
3. Credentials → Create Service Account → Create Key (JSON)
4. Сохранить как `credentials.json`
5. Расшарить **обе** таблицы (канон + xls-зеркало) на email вида
   `xxx@project.iam.gserviceaccount.com` — **Editor**, если хотите запись
   (/add, статусы, reconcile). Reader хватит только для чтения канона.

**Вариант 2:** CSV export — если таблица доступна «Anyone with the link», бот попробует скачать без credentials (только чтение, без автостатусов).

## Локальный запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # заполнить
python -m bot.main
```

## Структура

```
bot/
  main.py        — точка входа, меню команд, graceful shutdown
  handlers.py    — команды, кнопки, пагинация
  scheduler.py   — планировщик + рассылки
  monitor.py     — сравнение с предыдущим состоянием
  sheets.py      — чтение Google Sheets (retry + threadpool)
  storage.py     — SQLite (WAL, батч-снапшоты)
  formatters.py  — сообщения (HTML-escape, пагинация)
  keyboards.py   — inline и reply клавиатуры
  dates.py       — разбор дат и периодов
  utils.py       — безопасная отправка/редактирование
data/            — SQLite база (volume)
```

## Безопасность

- **Не коммитьте** `.env` и `credentials.json`
- Токен бота и ключи храните только на сервере
- При утечке токена — перевыпустите через @BotFather (`/revoke`)
- Архивы отчётов доступны администратору и роли `ib_operator`
- Контейнер работает от непривилегированного пользователя и имеет healthcheck

## Проверки и разработка

```bash
pip install -r requirements-dev.txt
ruff check bot tests
pytest -q
mypy bot
```

GitHub Actions запускает эти проверки для каждого push и pull request.
